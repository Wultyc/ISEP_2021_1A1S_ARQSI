import { Route } from '../route';

export class RoutesMapper {
    constructor() { }
}
//     fromFormToDTO = function (formBody: any, object: Route) {
//             object.distance = formBody.shortName,
//             object.durantion = formBody.name,
//             object.isEmptyRoute = formBody.longitude,
//             object.isReinforcmentRoute = formBody.latitude,
//             formBody.routeNodes.forEach(element => {
//                 element.
//             });
//         return object;
//     }
    
//     fromResponseToDto = function (model: Nodes, response: any) {
//             model.shortName = response.shortName,
//             model.name = response.name,
//             model.longitude = response.longitude,
//             model.latitude = response.latitude,
//             model.collectionNode = response.collectionNode,
//             model.surrenderNode = response.surrenderNode
        
//         return response;
//     }
// }